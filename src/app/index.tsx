import Login from "@moeum/features/user/login/Login";
import SignUp from "@moeum/features/user/sigin-up/SiginUp";

export default function LoginScreen() {
  return <Login />;
}

const styles = StyleSheet.create({
  container: {
    flex: 1
  }
});
